
package com.example


class Process {
  def main(args: Array[String]) = {
    // construct model source

    // iterate through it N times

    // send each json string to akka validator -> matcher -> transformer -> store or -> errorqueue


  }
}